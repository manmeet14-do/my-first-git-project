name: My first workflow



on:
 workflow_dispatch:



jobs:
   hello:
     runs-on: ubuntu-latest

     env:
       APP_NAME: my_app
       ENVIRONMENT: dev

     env:
       USERNAME: echo "Username is ${{ secrets.DOCKER_USERNAME }}"
       

     steps:
      - name: testing
        run: |
          echo "Application: $APP_NAME"
          echo "Environment: $ENVIRONMENT"

   build:
     needs: hello
     runs-on: ubuntu-latest

     steps:
      - name: bulding
        run: echo "Building"
 
       
   deploying:
     needs: build
     runs-on: ubuntu-latest

     steps:
       - name: deploying
         run: echo "deploying"
     





