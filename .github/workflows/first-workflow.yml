name: leaving checkout


on:
 push:
   branches:
     - main


jobs:
   demo:
     runs-on: ububtu-latest


   steps:
     - name: checkout repo
       uses: actions/checkout@v4


     - name: print current directory
       run: pwd
   
     
     - name: list files
       run: ls -la

     - name: show git version
       run: git --version



 
